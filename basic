python code :

conda create -p ./venv python=3.9 -y       # Create environment with a path
conda activate ./venv                      # Activate environment by path
conda info                                 # Check active environment
conda env list                             # List all conda environments
conda create -n myenv python=3.9 -y        # Create environment with a name
conda activate myenv                       # Activate environment by name
conda deactivate                           # Deactivate the active environment
conda remove -p ./venv --all               # Remove environment by path
conda remove -n myenv --all                # Remove environment by name
conda list                                 # List installed packages
conda install package_name                 # Install a package in active environment
conda update --all                         # Update all packages in active environment
